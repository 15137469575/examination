<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.upc.examination.mapper.QuestionPaperMapper">


    <insert id="insert" parameterType="org.upc.examination.entity.QuestionPaper">
        insert into question_paper
        (question_paper_id,paper_id,question_id,question_paper_sort,remark)
        values
        (#{questionPaperId},#{paperId},#{questionId},#{questionPaperSort},#{remark})

    </insert>

    <select id="selectByPaperId" resultType="org.upc.examination.entity.QuestionPaper">
        select question_paper_id,paper_id,question_id,question_paper_sort,remark
        from question_paper
        where paper_id =#{paperId}
    </select>

</mapper>